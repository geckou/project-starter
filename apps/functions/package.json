{
  "name": "@geckou/functions",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:firebase-admin --external:firebase-functions --external:express --external:cors",
    "dev": "yarn build && firebase emulators:start --only functions",
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "yarn build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint:fix": "eslint . --fix"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@geckou/shared": "*",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.0",
    "eslint": "^9.0.0",
    "firebase-functions-test": "^3.3.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
