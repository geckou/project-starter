# 確認事項キュー

> 判断が必要になったとき、**セッションを止めずにここへ積む**ためのファイル。
> 積んだ作業は保留し、別の作業へ移る（判断の基準は CLAUDE.md「自律性の境界」）。
> ユーザーはここを見て**まとめて答える**（`/questions`）。回答済みの項目は下のセクションへ移す。

積むときのルール:

- **1項目 = 1判断**。「ついでに」を混ぜない（まとめて答えられなくなる）
- **推奨案を必ず書く**。ユーザーが「それで」と一言で返せる状態にする
- **ブロック**に「これが決まらないと何が進まないか」を書く。回答の優先順位はここで決まる
- 推測で決めて実装を進めない。保留した作業は、回答が出てから着手する

---

## 未回答

<!--
以下のテンプレートで追記する。回答が済んだら「回答済み」セクションの先頭へ移す。

### Q-001 一行で書いた問い

- 発生: YYYY-MM-DD / どの作業中に出たか
- 種別: 仕様 / データモデル / セキュリティ / 依存 / UI / その他
- 状況: 何をどこまで作っていて、なぜ判断が要るのか（2〜3行）
- 選択肢:
  - A) 案（推奨）— 理由
  - B) 案 — 理由
- ブロック: この回答が出るまで着手できない作業（機能名・タスク名）
- 進めた範囲: 保留の手前までに済ませたこと（あれば）
-->

### Q-005 「管理下にあるリポジトリへの PR」は自律性の境界の 3 段目か

- 発生: 2026-09-06 / Issue #278（memory/ の廃止）の反映中
- 種別: その他（プロセス）
- 状況: 廃止した `memory/short-term/asking-permission-too-often.md` は、ユーザーからの指摘
  （「いちいち許可取らなくていいんだけど」）を受けて **「自分たちの管理下にあるリポジトリへの
  PR の作成は 3 段目に入らない。マージは人が決めるので、PR を出す時点では取り消せる」** と
  書いていた。一方 CLAUDE.md「自律性の境界」3 段目は今も
  **「このリポジトリ以外への Issue・PR の投稿」** を止める対象にしている。
  記録を反映すると 3 段目が緩むため、勝手に決めずに保留した
  （反映した「3 段目を広く取りすぎない」の一文は、設計判断についてのみ書いてある）。
- 選択肢:
  - A) 3 段目を「**管理下にない**リポジトリへの Issue・PR の投稿」に緩め、「管理下にあるリポジトリへの
    PR の作成は 3 段目ではない（マージは人が決めるので取り消せる）」を明記する（推奨）—
    指摘そのものへの対応になる。「管理下」の線引きは「geckou の Organization にあるか」で決まる
  - B) 現状のまま（このリポジトリ以外は全て確認する）。安全側だが、同じ指摘がまた出る
- ブロック: なし（どちらでも今の作業は進む）。回答が出たら CLAUDE.md「自律性の境界」を直す
- 進めた範囲: 「3 段目を広く取りすぎない」の一文までは反映済み

---

## 回答済み

### Q-006 `.github/` 配下を層マニフェストに登録するか

- 回答: 2026-09-07 / **A（層に属さないものとして明示する）を採用**
- 判断の理由:
  - `.github/` の中身（CI・デプロイ・ブランチ保護・PR / Issue テンプレート・テンプレート同期）は、
    どの層を選んだ派生プロジェクトでも同じように要る。層構成で要否が変わらない
  - `files` は「その層と一緒に消えるもの」の一覧であって、リポジトリのファイル一覧ではない。
    消えないものを載せると、一覧の意味が「登録漏れを探す対象」に変わってしまう
  - B（core へ登録）は core が `removable: false` なので動作が変わらず、
    表示上の意味だけが増える
- 反映済み: `.claude/docs/layers.md`「マニフェストの書き方」に
  「何を登録し、何を登録しないか」を追記（判断基準は
  **その層を外したときに消えるべきか**）。`layers.json` は変更なし
- 再考の目安: 層によって `.github/` のファイルごと要否が変わるものが出てきたら、
  そのファイルだけ該当層の `files` へ入れる

### Q-002 memory/（進化的メモリシステム）を読む経路を作るか、システムごと廃止するか

- 回答: 2026-09-06 / **B（廃止する）を採用**
- 判断の理由:
  - 実態を数えると 4 件しかなく、成功例は 1 件（git 運用のルールが pain_count 5 で
    `pre-git-guard.sh` になった）。残り 3 件のうち 1 件は既に CLAUDE.md へ、
    2 件はどこにも反映されないまま置かれていた
  - 閾値（pain_count 2 / 3）は既に運用されていない。`generated-issue-too-dense.md` は
    pain_count 1 のまま CLAUDE.md へ昇格しており、ファイル自身が
    「ユーザーからの直接の改善依頼のため、通常の昇格条件を待たずに追加した」と書いている。
    実際の運用は「ユーザーが直接言ったら即反映」で、階層と閾値は形だけ残っていた
  - A（SessionStart で読む）は経路は作れるが、溜まるほど毎ターンの文脈コストになる。
    反映先（Hook / スキル / CLAUDE.md）へ直接書くほうが、強制力も可読性も上
- 反映済み:
  - `memory/` と `memory/evolution.md` を削除
  - CLAUDE.md「進化的メモリシステム」節を削除。未反映だった 2 件は蒸留して
    「自律性の境界」（3 段目を広く取りすぎない）と「報告する事実は確認してから書く」に反映
  - `/wrap-up` の「メモリ記録」を「フィードバックの反映」に置き換え
    （その場で Hook / スキル / CLAUDE.md のどれに書くかを決める）
  - `/init-project`・README・`layers.md`・フックのコメント・`.templatesyncignore` を追従
- 反映しきれなかったもの:
  - `git-and-commit-rules-ignored.md` の未実装の申し送り（push 済みで PR が無いまま終了するのを
    止める Stop フック）→ Issue #287 へ移した
  - `asking-permission-too-often.md` の「管理下にあるリポジトリへの PR は 3 段目ではない」は
    CLAUDE.md の 3 段目と矛盾するため、Q-005 として積み直した
- 再考の目安: 「同じ指摘を 3 回受けたが、どこにも書く場所が無い」状況が出たら、
  記録の置き場をもう一度考える

### Q-003 CLAUDE.md をどこまで削り、何を `.claude/docs/` へ移すか

- 回答: 2026-09-06 / **A（Issue #282 の移動候補どおり分割する）を採用**
- 判断の理由:
  - CLAUDE.md は毎ターン読み込まれるコストがあり、本体保守者にしか関係しない節
    （上流報告・npm 配布・Renovate・参照切れ検出）がその大半を占めている
  - B（マーカーで区別するだけ）は行数が減らないため、毎ターンのコストは変わらない
  - C（同期単位も分ける）は `.templatesyncignore` の設計変更を伴い、分割そのものより影響が大きい。
    分割を先に済ませてから、必要なら別途扱う
- 反映: Issue #282 で実施する（別ブランチ・別 PR）
- 目標: CLAUDE.md 300 行以下。削った内容は `.claude/docs/` から辿れること。`yarn check:docs` が通ること

### Q-004 デプロイの鍵を GitHub Environment で staging / production に分けるか

- 回答: 2026-09-06 / **A（共通鍵のまま。分け方だけ書いておく）を採用**
- 判断の理由:
  - テンプレートは 1 つの Firebase プロジェクトに複数環境を相乗りさせる構成も想定しており、
    GitHub Environment を前提にすると scaffold 直後に動かない派生が出る
  - 鍵が全ステップから読める問題（本来の危険）は #274 で解消済み。
    残っていたのは「環境間で同じ鍵」という運用上の粒度の話で、危険度が一段違う
- 反映済み:
  - `.github/workflows/deploy.yml` — 鍵を参照する `env:` は認証ステップだけ。
    有無の判定は `secrets.X != ''` の真偽値
  - `.claude/docs/git-workflow.md` — 鍵が全環境共通であることと、分けたい場合の手順（Environment secret）
- 再考の目安: 派生プロジェクトで staging の事故が本番へ波及したら B に切り替える

### Q-001 使わないプロバイダのシークレットもダミーで作らせるか、宣言側を可変にするか

- 回答: 2026-09-06 / **A（このまま。使わない側はダミー値で作る）を採用**
- 判断の理由:
  - B（`BILLING_PROVIDERS` を足す）は既定を「両方」にする限り、結局 3 つ作らないと
    デプロイが落ちる。既定を変えれば今度は課金が黙って 503 になる。
    「知らないと踏む」点が変わらないのに、設定のノブだけ増える
  - C（プロバイダごとに関数を分ける）はエンドポイントの URL 構成とデプロイ単位が変わる。
    直す対象（秘密の置き場）に対して影響範囲が大きすぎる
  - A は追加の設定がゼロで、踏んだときのエラーが firebase-tools 側から
    「どの秘密が無いか」を名指しで出る。手順を 1 行増やすだけで足りる
- 反映済み:
  - `.claude/docs/billing.md` 1-2 節に手順（`printf '' | firebase functions:secrets:set X --data-file -`）
  - 同ファイルの完了チェックリストに「使わないプロバイダの分もダミー値で作った」
  - `apps/functions/src/lib/billing.ts` の `BILLING_SECRETS` にも同じ注意
- 再考の目安: 「片方だけ使う」構成が実際に増えて、ダミー作成の漏れが繰り返し起きるようなら B を入れる

<!--
### Q-000 問い

- 回答: YYYY-MM-DD / ユーザーの判断
- 反映: 実装・spec.md への反映内容
-->
