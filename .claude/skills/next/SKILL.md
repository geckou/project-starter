---
name: next
description: ロードマップから次に着手すべきタスクを選定して提案する
---

# next

CLAUDE.md の「次何をすればいい？」の手順を実行する。

## 手順

0. `.claude/docs/questions.md` の「回答済み」に、**保留のまま再開されていない作業**がないか見る。
   あればそれを最優先で提案する（前提が解けている＝すぐ進められる作業のため）
1. `.claude/docs/roadmap.md` の**機能ステータス表**を読み、現在のバージョンの「未着手」の機能を列挙する。
   あわせて **open な Issue**（`gh issue list`、または GitHub MCP）も候補に入れる。
   Issue は「やると決まっているが今はやらない」もの（CLAUDE.md「見つけた問題は Issue に切る」）
2. **フェーズ順**（1_基盤 → 2_バックエンド → 3_フロントエンド → 4_結合）で最も早いフェーズのものに絞る
3. その中で**前提機能が全て「完了」**のものに絞る（依存が解消されていない機能は選ばない）
4. 優先度 **「必須」→「重要」→「任意」** の順、同率なら他の機能から依存されている数が多い順に1つ選ぶ
5. 機能ステータス表の「関連仕様」に列挙された `spec.md` のセクション（画面・データモデル・API）を読む
6. Figma リンクがあればデザインを確認する
7. 以下を宣言し、**そのまま着手する**（承認待ちで止まらない）:
   - 選んだ機能と選定理由（フェーズ・優先度・依存関係）
   - 作業内容と影響範囲（作成・変更するファイル）
   - 使うスキル（/new-collection, /new-function, /new-page 等）
   - 完了条件（Definition of Done）

## 候補の選び方（機能と Issue が並ぶとき）

機能の実装が先。Issue は**機能の進行を妨げているもの**（今のフェーズのコードに触る不具合等）
だけ割り込ませ、それ以外はフェーズの切れ目で処理する。1回の作業に複数の Issue を混ぜない。

## 候補がない場合

- 「未着手」も open な Issue もない → 「実装中」の機能の残作業を確認して提案する
- 前提機能が未完了のものしかない → ブロックしている前提機能を先に提案する
- 現在のバージョンが全て「完了」 → 次バージョンの計画（planning.md のコア機能一覧から機能ステータス表への転記）を提案する

## 注意

**選定も実装も、CLAUDE.md「自律性の境界」の3段階で判断する。** `/next` に固有の例外は作らない。

- 選んだ作業が **2（保留してキューへ）** に当たるなら、選び直して次の候補へ進む。
  候補が尽きた（＝進められる作業が無い）なら、そこで初めて聞く
- 選んだ作業が **3（その場で止めて聞く）** に触れるなら、着手前に確認する
- 選定そのものが決まらない場合（候補が同点で、どれを選ぶかで以降の作業順が大きく変わる）は、
  避けようがないので聞く。推奨を1つ示して短く聞く
- 着手したら、機能ステータス表の該当機能を「実装中」に更新してから実装を始める
- 実装中に判断が必要になったら、`.claude/docs/questions.md` に積んでその作業を保留し、
  別の作業へ移る（`/questions` で回収する）
